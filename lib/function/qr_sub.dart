import 'package:flutter/material.dart';

class QRSubPage extends StatelessWidget {
  const QRSubPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
